import fs from 'node:fs';import path from 'node:path';
const root=process.cwd(),dist=path.join(root,process.env.OUT_DIR||'dist'),BASE=process.env.SITE_BASE||'/';
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const abs=p=>`${BASE}${p}`.replace(/\/+/g,'/');
function htmlShell(title,body,extra=''){return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover"><title>${esc(title)}</title><meta name="robots" content="index,follow"><link rel="stylesheet" href="${abs('styles.css')}"><link rel="stylesheet" href="${abs('account.css')}"></head><body><header class="site-header"><a class="brand" href="${abs('')}" aria-label="Zero Trust home"><span class="brand-mark">ZT</span><span class="brand-name">Zero Trust</span></a><nav><a class="nav-link" href="${abs('tools/')}">Tools</a><a class="nav-link" href="${abs('pricing/')}">Pricing</a><a class="nav-link" href="${abs('security/')}">Security</a><button id="themeBtn" class="icon-btn" type="button">Dark</button><button id="menuBtn" class="icon-btn mobile-menu" type="button" aria-expanded="false">Menu</button></nav></header><main>${body}</main><script src="${abs('app.js?v=302')}" defer></script><script src="${abs('account.js?v=1')}" defer></script>${extra}</body></html>`}
const pages={
 'login/index.html':htmlShell('Log in — Zero Trust',`<section class="form-card zt-card"><div class="eyebrow">ACCOUNT</div><h1>Log in</h1><form id="loginForm" class="form-grid"><label>Email<input name="email" type="email" autocomplete="email" required maxlength="254"></label><label>Password<input name="password" type="password" autocomplete="current-password" required minlength="12"></label><div class="form-message" aria-live="polite"></div><button class="btn primary" type="submit">Log in</button></form><p class="small">New here? <a href="${abs('signup/')}">Create an account</a>.</p></section>`),
 'signup/index.html':htmlShell('Sign up — Zero Trust',`<section class="form-card zt-card"><div class="eyebrow">ACCOUNT</div><h1>Create account</h1><form id="signupForm" class="form-grid"><label>Email<input name="email" type="email" autocomplete="email" required maxlength="254"></label><label>Password<input name="password" type="password" autocomplete="new-password" required minlength="12"><small class="small">Use at least 12 characters.</small></label><div class="form-message" aria-live="polite"></div><button class="btn primary" type="submit">Sign up</button></form><p class="small">Already registered? <a href="${abs('login/')}">Log in</a>.</p></section>`),
 'account/index.html':htmlShell('Account — Zero Trust','<section class="page" data-account-page></section>'),
 'owner/index.html':htmlShell('Owner Control Room — Zero Trust','<section data-owner-page></section>',`<script src="${abs('owner.js?v=1')}" defer></script>`),
 'pricing/index.html':htmlShell('Pricing — Zero Trust',`<section class="page"><div class="eyebrow">SERVICES</div><h1>Free &amp; Pro</h1><p>Core local-first tools remain available without a subscription. Pro access is verified by the backend for protected services.</p><div class="pricing-grid"><article class="price-card"><div class="eyebrow">FREE</div><h2>Free</h2><div class="price">$0</div><ul><li>Core browser tools</li><li>Local processing where supported</li><li>No account required for public tools</li></ul><a class="btn" href="${abs('tools/')}">Explore tools</a></article><article class="price-card"><div class="eyebrow">PRO</div><h2>Pro</h2><div class="price">Coming soon</div><ul><li>Server-verified Pro services</li><li>Higher usage limits</li><li>Sync/history for supported services</li><li>Priority features</li></ul><a class="btn primary" href="${abs('signup/')}">Create account</a></article></div></section>`)
};
for(const [rel,content]of Object.entries(pages)){const f=path.join(dist,rel);fs.mkdirSync(path.dirname(f),{recursive:true});fs.writeFileSync(f,content)}
function walk(dir){for(const e of fs.readdirSync(dir,{withFileTypes:true})){const f=path.join(dir,e.name);if(e.isDirectory())walk(f);else if(e.isFile()&&e.name==='index.html')patch(f)}}
function patch(file){let h=fs.readFileSync(file,'utf8');if(h.includes('account.js?v=1'))return;h=h.replace('</head>',`<link rel="stylesheet" href="${abs('account.css')}"></head>`);h=h.replace('</body>',`<script src="${abs('account.js?v=1')}" defer></script></body>`);h=h.replace(/<a class="nav-link" href="[^\"]*tools\/">Search<\/a>/,`<a class="nav-link" href="${abs('tools/')}">Search</a><a class="nav-link" href="${abs('pricing/')}">Pricing</a>`);fs.writeFileSync(file,h)}
walk(path.join(dist,'tools'));patch(path.join(dist,'index.html'));console.log('Platform enhancement: account, pricing and owner surfaces added; account runtime injected into all tool pages.');
