import { mountRealTool } from './real-tools.js';
import { mount as mountIndividual } from './individual-tools.js';
import { mount as mountFormatCodec } from './image-codec.js';
import { mount as mountGenericImage } from './image-generic.js';
const exact=new Set(['resize image (pixels)','compress jpg','compress png','compress webp','rotate image','flip image','add watermark to image','circle crop for profile picture','image border adder','image flip vertical/horizontal']);
const converters=/^(jpg|png|webp|gif|bmp|svg|avif|tiff|heic|raw|pdf) to (jpg|png|webp|avif|gif|bmp|svg|tiff|heic|pdf) converter$/i;
const MAX=30*1024*1024;
function guard(){for(const input of document.querySelectorAll('#toolApp input[type=file]')){if(input.dataset.ztGuarded)return;input.dataset.ztGuarded='1';input.addEventListener('change',()=>{const bad=[...input.files].find(f=>f.size>MAX);if(bad){input.value='';const out=document.querySelector('#toolApp .output');if(out){out.className='output error';out.textContent='File exceeds the 30MB browser safety limit.'}}},{passive:true})}}
export async function mount(slug){let tools=window.ZT_TOOLS;if(!tools){const base=location.pathname.startsWith('/Zero-trust/')?'/Zero-trust/':'/';tools=await fetch(base+'data/tools-index.json',{cache:'force-cache'}).then(r=>r.json());window.ZT_TOOLS=tools}const meta=(tools||[]).find(x=>x.slug===slug),name=meta?.name||slug.replace(/-/g,' ');if(converters.test(name)||exact.has(name.toLowerCase()))await mountFormatCodec(slug);else if(/grayscale|gray|sharpen|blur|pixelate|flip|sepia/u.test(name.toLowerCase()))await mountGenericImage(slug);else if(name.toLowerCase().includes('watermark')||name.toLowerCase().includes('border'))await mountRealTool(slug);else await mountIndividual(slug);guard()}