const PDFJS=()=>import('https://cdn.jsdelivr.net/npm/pdfjs-dist@4.10.38/build/pdf.mjs').then(m=>{m.GlobalWorkerOptions.workerSrc='https://cdn.jsdelivr.net/npm/pdfjs-dist@4.10.38/build/pdf.worker.mjs';return m});
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
export async function mount(){const app=document.querySelector('#toolApp');if(!app)return;app.innerHTML='<div class="tool-box"><label>PDF file<input id="file" type="file" accept="application/pdf"></label><label>Search text<input id="query" type="text" placeholder="Enter text to find"></label><div class="tool-actions"><button class="btn primary" id="run" type="button">Run tool</button></div><pre class="output" id="status" data-status="idle">Ready.</pre></div>';const file=document.querySelector('#file'),query=document.querySelector('#query'),status=document.querySelector('#status');document.querySelector('#run').onclick=async()=>{try{if(!file.files[0])throw Error('Select a PDF first.');const needle=query.value.trim().toLowerCase();if(!needle)throw Error('Enter search text.');status.dataset.status='working';status.textContent='Searching PDF…';const m=await PDFJS(),doc=await m.getDocument({data:new Uint8Array(await file.files[0].arrayBuffer()),disableWorker:true,isEvalSupported:false}).promise,rows=[];for(let i=1;i<=doc.numPages;i++){const page=await doc.getPage(i),content=await page.getTextContent(),text=content.items.map(x=>x.str).join(' ');if(text.toLowerCase().includes(needle))rows.push(`Page ${i}: ${text}`)}status.dataset.status='success';status.textContent=rows.length?rows.join('\n\n'):'No matching text found.'}catch(e){status.dataset.status='error';status.textContent=e.message||'PDF search failed.'}}}
