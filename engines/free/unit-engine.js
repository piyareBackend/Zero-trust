const EPS=Number.EPSILON;
const R={length:{base:'meter',ratios:{meter:1,m:1,cm:.01,centimeter:.01,mm:.001,millimeter:.001,km:1000,kilometer:1000,inch:.0254,in:.0254,ft:.3048,foot:.3048,yd:.9144,yard:.9144,mile:1609.344}},mass:{base:'gram',ratios:{gram:1,g:1,kg:1000,kilogram:1000,mg:.001,milligram:.001,lb:453.59237,pound:453.59237,oz:28.349523125,ounce:28.349523125}},area:{base:'square_meter',ratios:{square_meter:1,m2:1,cm2:.0001,km2:1e6,square_kilometer:1e6,square_foot:.09290304,ft2:.09290304,acre:4046.8564224,hectare:10000}},volume:{base:'liter',ratios:{liter:1,l:1,ml:.001,milliliter:.001,cubic_meter:1000,m3:1000,gallon:3.785411784,quart:.946352946,cup:.2365882365,pint:.473176473}},speed:{base:'mps',ratios:{mps:1,kmh:1/3.6,kph:1/3.6,mph:.44704,knot:.514444444}},data:{base:'byte',ratios:{bit:.125,byte:1,kb:1024,mb:1048576,gb:1073741824,tb:1099511627776,kib:1024,mib:1048576,gib:1073741824}},time:{base:'second',ratios:{second:1,minute:60,hour:3600,day:86400,week:604800}},temperature:{base:'celsius',ratios:{celsius:'c',fahrenheit:'f',kelvin:'k'}}};
const clean=x=>String(x??'').toLowerCase().trim().replace(/[\s-]+/g,'_');
export function convert(value,category,from,to){const n=Number(String(value).replace(/[₹,$,%\s]/g,''));if(!Number.isFinite(n))throw Error('Enter a valid number.');const d=R[category];if(!d)throw Error(`Unsupported unit category: ${category}`);const a=clean(from),b=clean(to);if(category==='temperature'){if(!d.ratios[a]||!d.ratios[b])throw Error('Unsupported temperature unit.');const c=a==='c'?n:a==='f'?(n-32)*5/9:n-273.15;const raw=b==='c'?c:b==='f'?c*9/5+32:c+273.15;const rounded=Math.round((raw+EPS)*1e12)/1e12;return Object.is(rounded,-0)?0:rounded}const x=d.ratios[a],y=d.ratios[b];if(!Number.isFinite(x)||!Number.isFinite(y)||y===0)throw Error('Unsupported unit.');const raw=(n*x)/y,rounded=Math.round((raw+EPS)*1e12)/1e12;return Object.is(rounded,-0)?0:rounded}
export function categoryFromSlug(slug=''){const s=slug.toLowerCase();if(/temperature/.test(s))return'temperature';if(/mass|weight/.test(s))return'mass';if(/area/.test(s))return'area';if(/volume|liter|gallon/.test(s))return'volume';if(/speed|velocity/.test(s))return'speed';if(/data|storage|byte/.test(s))return'data';if(/time|duration/.test(s))return'time';return'length'}
export function mount(slug){const app=document.querySelector('#toolApp');if(!app)throw Error('Tool container missing.');const c=categoryFromSlug(slug),units=Object.keys(R[c].ratios);app.innerHTML=`<div class="zt-card"><div class="zt-local">LOCAL-FIRST · deterministic unit engine</div><div class="control-grid"><label>Value<input id="ueValue" type="text" inputmode="decimal" value="1"></label><label>From<select id="ueFrom">${units.map(u=>`<option>${u}</option>`).join('')}</select></label><label>To<select id="ueTo">${units.map(u=>`<option>${u}</option>`).join('')}</select></label></div><button class="btn primary" id="ueRun">Convert</button><div class="output" id="ueOut">Ready.</div></div>`;document.querySelector('#ueRun').onclick=()=>{try{const r=convert(document.querySelector('#ueValue').value,c,document.querySelector('#ueFrom').value,document.querySelector('#ueTo').value);document.querySelector('#ueOut').textContent=String(r);document.querySelector('#ueOut').className='output success'}catch(e){document.querySelector('#ueOut').textContent=e.message;document.querySelector('#ueOut').className='output error'}}}
