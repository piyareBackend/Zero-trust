(async()=>{const base=location.pathname.startsWith('/Zero-trust/')?'/Zero-trust/':'/';const slug=location.pathname.split('/').filter(Boolean).pop()||'';const app=document.querySelector('#toolApp');try{let tools=window.ZT_TOOLS;if(!tools){const r=await fetch(base+'data/tools-index.json');if(r.ok)tools=await r.json();window.ZT_TOOLS=tools||[]}const meta=(tools||[]).find(x=>x.slug===slug);if(!meta)throw Error('Tool metadata unavailable');if(meta.phase===2){const gate=await fetch(`${base}api/tools/${encodeURIComponent(slug)}`,{credentials:'same-origin',cache:'no-store'});const gd=await gate.json().catch(()=>({}));if(!gate.ok||gd.allowed!==true){if(app)app.innerHTML=`<div class="output pro-gate"><strong>Pro service</strong><p>${gd.reason==='pro_required'?'This network service requires a server-verified Pro entitlement.':'This service is currently unavailable.'}</p><a class="btn primary" href="${base}pricing/">View plans</a></div>`;return}}const name=meta.name||'';let file;if(/hashtag generator|instagram bio generator|youtube title\/tag generator|caption generator|emoji picker\/finder/i.test(name))file='social-tools.js';else if(meta.engine==='image')file='strict-image.js';else if(meta.engine==='pdf')file='strict-pdf.js';else if(meta.engine==='document')file='document-converter.js';else if(meta.engine==='audio'||meta.engine==='video')throw Error('Audio/video conversion requires a bundled media codec. No fake/pass-through conversion is exposed.');else file='extended.js';const m=await import(`${base}engines/free/${file}?v=401`);if(!m||typeof m.mount!=='function')throw Error('Tool engine mount() missing');await m.mount(slug)}catch(e){console.error('[Zero Trust] engine',e);if(app)app.innerHTML='<div class="output error"><strong>Tool unavailable.</strong><br>'+String(e.message||e)+'</div>'}})();
