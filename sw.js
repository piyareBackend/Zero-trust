const CACHE='zt-shell-v7';
const ASSETS=['/Zero-trust/','/Zero-trust/index.html','/Zero-trust/styles.css','/Zero-trust/app.js','/Zero-trust/tool-engine.js','/Zero-trust/engines/free/core.js','/Zero-trust/manifest.json'];
self.addEventListener('install',e=>e.waitUntil(caches.open(CACHE).then(c=>c.addAll(ASSETS)).then(()=>self.skipWaiting())));
self.addEventListener('activate',e=>e.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(k=>k!==CACHE).map(k=>caches.delete(k)))).then(()=>self.clients.claim())));
self.addEventListener('fetch',e=>{if(e.request.method!=='GET')return;const u=new URL(e.request.url);const local=u.origin===location.origin;const fresh=local&&(u.pathname.endsWith('.js')||u.pathname.endsWith('.html')||u.pathname.endsWith('.css'));if(fresh){e.respondWith(fetch(e.request).then(r=>{if(r.ok)caches.open(CACHE).then(c=>c.put(e.request,r.clone()));return r}).catch(()=>caches.match(e.request)));return}e.respondWith(caches.match(e.request).then(c=>c||fetch(e.request).then(r=>{if(r.ok&&local)caches.open(CACHE).then(x=>x.put(e.request,r.clone()));return r}).catch(()=>c)))})