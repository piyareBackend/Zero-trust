{
  "name": "zero-trust-mobile",
  "private": true,
  "scripts": {
    "build:web": "node ../scripts/build-cloudflare.mjs && node ../scripts/enhance-platform.mjs",
    "sync": "npm run build:web && cap sync",
    "android": "npm run sync && cap open android",
    "ios": "npm run sync && cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "@capacitor/android": "^8.0.0",
    "@capacitor/ios": "^8.0.0"
  }
}
